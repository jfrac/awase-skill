name: Security audit

on: [push, pull_request]

jobs:
  snyk-agent-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uvx snyk-agent-scan@latest skills/awase
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
